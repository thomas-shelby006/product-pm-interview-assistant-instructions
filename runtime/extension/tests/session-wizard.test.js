import test from 'node:test'; import assert from 'node:assert/strict'; import { deriveSessionWizard } from '../dashboard/session-wizard-model.js';
test('Cycle 10 start and end wizards preserve guarded boundaries',()=>{ const start=deriveSessionWizard({preflightWizard:{steps:[{id:'roles',label:'Roles',complete:true},{id:'self',label:'Self test',complete:false,command:'run_self_test'}]},production:{operatingProfile:{label:'Balanced'}},productionControls:{operatingProfile:'balanced'}},'start'); assert.equal(start.current.id,'self'); assert.equal(start.ready,false); const end=deriveSessionWizard({endGuard:{counts:{actionable:1,inFlight:0,unpersisted:0}},production:{diagnostics:{privacy:{safe:true}}}},'end'); assert.equal(end.current.id,'delivery'); assert.equal(end.ready,false); });

test('Assist workspace and persistent Action Dock are packaged',async()=>{ const {readFile}=await import('node:fs/promises'); const html=await readFile(new URL('../dashboard/index.html',import.meta.url),'utf8'); const js=await readFile(new URL('../dashboard/dashboard.js',import.meta.url),'utf8'); for(const id of ['panelAssist','liveActionDock','choiceWorkspace','assistTriageItems','assistRouteState','assistRecoveryState','assistCommandHistory','assistForecastState','assistPolicyTitle','assistWizardTitle']) assert.match(html,new RegExp(`id="${id}"`)); assert.match(js,/renderLiveAssist/); assert.match(js,/resolve_operator_choice/); });