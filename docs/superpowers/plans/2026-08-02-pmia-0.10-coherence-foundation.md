# PMIA 0.10 Control-Plane Coherence Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Eliminate command/control drift, unsafe implicit decisions, release-evidence blind spots, duplicate dashboard IDs, and active-version inconsistency without changing lossless transport ownership.

**Architecture:** Add a metadata-only shared command registry consumed by protocol normalization, dashboard rendering, command search, and reachability audits. Existing controller/content-runtime cases remain mutation owners. Decision actions distinguish execute, choose, and inspect. Release evidence requires both Pilot and Production UI proof.

**Tech Stack:** Manifest V3 extension JavaScript, Node test runner, PowerShell release tooling, HTML/CSS dashboard, AutoHotkey launchers.

## Global Constraints

- Preserve sender outbox, delivery ledger, sequence admission, protected batching, rendered proof, session-only state, and normal-Edge isolation.
- No new transport, persistent store, recovery owner, external dependency, cloud service, or automatic ambiguous choice.
- Write regression contracts during development; defer executable tests to the foundation gate.
- Work only in `C:\Users\Sundar\Documents\product-pm-interview-assistant-improvement`.
- No push, merge, tag, or installed-extension replacement.

---

### Task 1: Canonical command registry

**Files:**
- Create: `runtime/extension/shared/operator-command-registry.js`
- Modify: `runtime/extension/shared/dashboard-protocol.js`
- Modify: `runtime/extension/shared/operator-command-catalog.js`
- Test: `runtime/extension/tests/command-registry.test.js`

**Interfaces:**
- Produces `COMMAND_REGISTRY`, `commandDefinition(id)`, `registeredCommandIds()`, `normalizeRegisteredCommand(value)`, and `auditCommandRegistry()`.
- Each definition contains `id`, `label`, `group`, `risk`, `mode`, `owner`, `payload`, `confirmation`, and `navigation`.

- [ ] Write contracts proving unique IDs, valid risk/mode/owner values, `compact_proven` and `retry_outbox` registration, and deterministic registry order.
- [ ] Implement immutable registry definitions for every current dashboard command.
- [ ] Generate `DASHBOARD_COMMANDS` from registry IDs while retaining command-specific payload sanitizers.
- [ ] Adapt command catalog/search to registry metadata without changing availability logic.
- [ ] Add registry digest generation for release evidence.
- [ ] Commit: `feat: centralize PMIA command definitions`.
### Task 2: End-to-end command reachability audit

**Files:**
- Create: `runtime/extension/shared/command-reachability-audit.js`
- Modify: `runtime/extension/tests/validation.test.js`
- Modify: `runtime/extension/tests/dashboard-usability.test.js`
- Test: `runtime/extension/tests/command-reachability.test.js`

**Interfaces:**
- Consumes registry definitions, dashboard markup, controller source, and content-command source.
- Produces `{ ok, duplicateDomIds, visibleWithoutRegistry, registryWithoutOwner, controllerWithoutRegistry, unboundControls }`.

- [ ] Write failing contracts for the current duplicate `openShortcutHelp` ID and the two protocol-missing commands.
- [ ] Implement source/markup audit helpers with deterministic sorted output.
- [ ] Enforce exactly one owner for every executable registry command.
- [ ] Enforce every visible dashboard command is registered and every explicit button ID is unique.
- [ ] Integrate the audit into the extension validator.
- [ ] Commit: `test: enforce command reachability invariants`.

### Task 3: Repair current control defects

**Files:**
- Modify: `runtime/extension/dashboard/index.html`
- Modify: `runtime/extension/dashboard/dashboard.js`
- Modify: `runtime/extension/shared/dashboard-protocol.js`
- Modify: `runtime/extension/shared/runtime-pilot-controller.js`
- Test: `runtime/extension/tests/dashboard-usability.test.js`
- Test: `runtime/extension/tests/runtime-pilot-controller.test.js`

**Interfaces:**
- `compact_proven` archives only proven ledger history through the existing controller owner.
- `retry_outbox` invokes the sender outbox retry owner through the standard command route.

- [ ] Rename the lower duplicate shortcut-help trigger and bind it through the shared focus coordinator.
- [ ] Route `compact_proven` and `retry_outbox` through registry-generated protocol validation.
- [ ] Add command-result tests for success, unavailable owner, timeout, and replay.
- [ ] Verify no direct UI mutation bypasses `sendCommand()`.
- [ ] Commit: `fix: restore dead Pilot controls and unique IDs`.

### Task 4: Explicit-choice decision safety

**Files:**
- Create: `runtime/extension/shared/operator-choice-model.js`
- Modify: `runtime/extension/shared/operator-decision-center.js`
- Modify: `runtime/extension/shared/runtime-pilot-state.js`
- Modify: `runtime/extension/shared/runtime-pilot-controller.js`
- Test: `runtime/extension/tests/operator-choice-model.test.js`
- Test: `runtime/extension/tests/operator-decision-center.test.js`

**Interfaces:**
- Produces `deriveOperatorChoice(snapshot)`, `recordChoiceSelection(...)`, and `clearResolvedChoice(...)`.
- Decision items expose `actionMode: execute|choose|inspect`; choose items contain no default command payload.

- [ ] Write failing tests showing no-response and draft-conflict items cannot execute a default option.
- [ ] Add durable metadata-only choice state with snapshot fingerprint and expiry.
- [ ] Make Decision Center rank choice-required items but navigate instead of mutate.
- [ ] Reject stale selections after the underlying batch/conflict fingerprint changes.
- [ ] Preserve unresolved choices through reconnect and worker restart.
- [ ] Commit: `fix: require explicit operator choices`.
### Task 5: Policy-impact preview contract

**Files:**
- Create: `runtime/extension/shared/policy-impact-preview.js`
- Modify: `runtime/extension/shared/operating-profile.js`
- Modify: `runtime/extension/shared/containment-status.js`
- Modify: `runtime/extension/shared/runtime-pilot-state.js`
- Test: `runtime/extension/tests/policy-impact-preview.test.js`

**Interfaces:**
- Produces `buildPolicyImpactPreview(snapshot, request, now)` and `validatePolicyImpactConfirmation(snapshot, preview, now)`.
- Preview includes fingerprint, expiry, affected controls, protected count, provider-write transition, post-answer effect, reversibility, risk, and blockers.

- [ ] Write tests for Safe/Balanced/Fast transitions and containment override.
- [ ] Require confirmation for provider-write activation, drain-all, or containment override.
- [ ] Reject confirmation when snapshot fingerprint, protected count, eligibility, or expiry changes.
- [ ] Store only preview metadata and selected policy ID.
- [ ] Commit: `feat: add stale-safe policy impact previews`.

### Task 6: Release-evidence closure

**Files:**
- Modify: `runtime/extension/scripts/build-release-evidence.mjs`
- Modify: `runtime/extension/scripts/run-isolated-release-smoke.ps1`
- Modify: `runtime/extension/scripts/isolated-release-runner.mjs`
- Modify: `runtime/extension/tests/release-evidence.test.js`
- Modify: `runtime/extension/tests/isolated-release-smoke.test.js`

**Interfaces:**
- Release evidence requires `pilotUiOk`, `productionUiOk`, command-registry digest, command-reachability audit, privacy, cleanup, and exact commit identity.

- [ ] Add failing evidence tests for absent or false `productionUiOk`.
- [ ] Preserve Production desktop/320/280/print/accessibility/control details in the manifest.
- [ ] Add command-registry digest and reachability result to evidence and handoff output.
- [ ] Fail closed on visible unreachable controls or duplicate IDs.
- [ ] Commit: `fix: close production release evidence gaps`.

### Task 7: Active version and setup alignment

**Files:**
- Modify: `runtime/extension/manifest.json`
- Modify: `README.md`
- Modify: `AI_SYSTEM_CONTEXT.md`
- Modify: `docs/CURRENT_STATUS_DASHBOARD.md`
- Modify: `docs/CURRENT_SETUP_HANDOFF_AND_REQUIREMENTS.md`
- Modify: `runtime/README_INSTALL_TEST.md`
- Modify: active release/version tests

**Interfaces:**
- Current runtime identity is `0.10.0` candidate; historical specs and migration fixtures remain unchanged.

- [ ] Update active manifest, installation, setup, status, and architecture wording.
- [ ] Remove active claims that call the current runtime 0.7 or 0.8.
- [ ] Add validation that active release surfaces agree with manifest version.
- [ ] Commit: `docs: align active PMIA 0.10 release surfaces`.

### Task 8: Foundation consolidated gate

**Files:**
- Update: `docs/PMIA_010_IMPROVEMENT_CYCLES.md`
- Update: `docs/superpowers/plans/2026-08-02-pmia-0.10-coherence-foundation.md`

- [ ] Run focused registry, reachability, decision, policy-preview, evidence, dashboard, controller, and version suites.
- [ ] Run complete `runtime/validate-extension.ps1` only after all foundation source work is complete.
- [ ] Fix failures at owning boundaries and rerun the complete gate.
- [ ] Record exact totals and commit hash.
- [ ] Commit: `test: verify PMIA 0.10 coherence foundation`.