# PMIA Runtime 0.5 Verification Record

## Release scope

PMIA runtime 0.5 removes avoidable blocking from provisional and final delivery, corrects Claude interruption semantics from the supplied exports, adds reload-safe preview streams, adds event-driven self-recovery, and adds an authorized preflight status hotkey.

## Evidence inputs

- Claude full export SHA-256: `2B7F2625CF1C0821284D5F8C4A689D6A8AEE7ADF91A37D1D51F377B674A47E1F`
- ChatGPT full export SHA-256: `B74D38A373DAC1A30742CBDCC6931709ED873A527703320D771C19F032581F6B`
- Detailed sanitized findings: `docs/evidence/2026-07-29-provider-runtime-evidence.md`

## Automated release gate

Executed from the isolated `feature/runtime-0.5-performance` worktree:

```powershell
npm test
npm run validate
powershell -NoProfile -ExecutionPolicy Bypass -File runtime\Validate_Extension_Runtime.ps1
git diff --check
```

Results:

- 143 tests passed; zero failed, skipped, or cancelled.
- 46 JavaScript files passed extension validation.
- AutoHotkey v2 launcher validation passed.
- Whitespace validation passed.
## Safety and scope guards

- Changed-file secret scan found no API-key, bearer-token, password, client-secret, or private-key patterns.
- No temporary implementation files remained in the worktree.
- The following older paths were byte-for-byte unchanged relative to `main`:
  - `runtime/Final_2_Window_Fixed.ahk`
  - `runtime/Session_Tracker_End_Session.ahk`
  - `runtime/tm_scripts`
  - `runtime/tm_update_support`
  - `archive`
- The older standalone repository ZIP and historical local files remain outside this release and are not cleanup targets.

## Critical regression coverage

- Claude `server_interrupt` preserves a growing utterance.
- Claude `transcript_empty` clears provisional text.
- Human Claude `message_complete` commits the exact preview turn.
- ChatGPT partial growth never submits during active voice.
- Ready receivers submit without a fixed delay.
- Sender reload starts a new preview stream without stale-sequence rejection.
- Preview stream and turn memory are bounded.
- Page/network/visibility restoration triggers immediate recovery.
- Back/forward-cache `pagehide` preserves runtime resources for `pageshow` recovery.
- Receiver acknowledgement does not wait for telemetry after successful submission.
- `Ctrl+Shift+F11` status requests are authorized through session ownership.

## Remaining manual release checks

The extension is deterministic under automated fixtures. A real provider-session gate still requires reloading the unpacked extension and exercising all four provider combinations, both native-voice senders, missing-receiver recovery, receiver reload, superseding, preflight display, and a long-session soak.
