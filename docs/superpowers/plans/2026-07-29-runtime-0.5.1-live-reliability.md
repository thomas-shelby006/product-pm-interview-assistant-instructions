# PMIA Runtime 0.5.1 Live Reliability Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Fix the live startup crash, add visible fatal diagnostics and active counterpart preflight, then pass the four-provider text matrix and publish runtime 0.5.1.

**Architecture:** Preserve the current MV3 router and provider adapters. Add one dependency-contract regression, one dependency-free emergency banner, and a non-invasive service-worker ping that proves the counterpart content runtime is alive without touching provider text or generation.

**Tech Stack:** Manifest V3, JavaScript ES modules, Node test runner, Chrome/Edge tabs messaging, AutoHotkey v2.

## Global Constraints

- No provider private APIs or raw-audio retention.
- No dependency installs or broad formatting.
- Preserve legacy rollback files and unrelated Edge windows.
- Preview remains disposable; final routing remains durable and sequenced.
- Implement with red-green-refactor and commit each independently testable slice.

---

### Task 1: Restore the receiver polling dependency

**Files:**
- Modify: `runtime/extension/tests/validation.test.js`
- Modify: `runtime/extension/content/entry.js`

**Interfaces:**
- Consumes: exported `sleep(ms)` from `content/runtime.js`
- Produces: `createReceiverController({ adapter, sleep, onStatus })` with a declared dependency
- [ ] **Step 1: Write the failing dependency-contract test**

Add a test that reads `entry.js`, extracts the `./runtime.js` import, and proves `sleep` is imported whenever the receiver controller is created with the `sleep` property.

- [ ] **Step 2: Run the focused test and confirm RED**

Run: `node --test runtime/extension/tests/validation.test.js`
Expected: FAIL because `sleep` is passed but not imported.

- [ ] **Step 3: Restore the minimal import**

Add `sleep` to the existing named import from `./runtime.js`. Do not change receiver timing behavior.

- [ ] **Step 4: Run focused and full tests**

Run the validation test, `npm test`, and `npm run validate`.

- [ ] **Step 5: Commit**

Commit message: `fix: restore PMIA receiver startup dependency`

### Task 2: Add a dependency-free fatal runtime banner

**Files:**
- Create: `runtime/extension/content/runtime-fatal.js`
- Modify: `runtime/extension/content/main.js`
- Modify: `runtime/extension/content/entry.js`
- Create: `runtime/extension/tests/runtime-fatal.test.js`
- Modify: `runtime/extension/manifest.json`
- Modify: `runtime/extension/tests/manifest.test.js`

**Interfaces:**
- Produces: `renderRuntimeFatal(doc, { stage, error, version })`
- Consumes: `chrome.runtime.getManifest().version` and no other runtime modules
- [ ] **Step 1: Write fatal-banner tests**

Test idempotent rendering, sanitized error text, stage/version display, and replacement of an earlier fatal message.

- [ ] **Step 2: Confirm RED**

Run: `node --test runtime/extension/tests/runtime-fatal.test.js`
Expected: module missing.

- [ ] **Step 3: Implement the banner and wire both failure paths**

`main.js` handles dynamic-import rejection. `entry.js` handles `startRuntime()` rejection. Both call the same small renderer and keep console diagnostics.

- [ ] **Step 4: Expose the module and verify GREEN**

Add `content/runtime-fatal.js` to web-accessible resources. Run focused tests, manifest tests, and validator.

- [ ] **Step 5: Commit**

Commit message: `feat: show PMIA fatal startup diagnostics`

### Task 3: Upgrade preflight to an active counterpart ping

**Files:**
- Create: `runtime/extension/shared/preflight.js`
- Modify: `runtime/extension/background.js`
- Modify: `runtime/extension/content/entry.js`
- Modify: `runtime/extension/shared/session-status.js`
- Create: `runtime/extension/tests/preflight.test.js`
- Modify: `runtime/extension/tests/session-status.test.js`
- Modify: `runtime/extension/tests/validation.test.js`

**Interfaces:**
- Produces: `runCounterpartPreflight({ registry, sessionId, requesterTabId, sendToTab, now, staleAfterMs })`
- Content message: `PMIA_PREFLIGHT_PING`
- Service-worker request: `PMIA_RUN_PREFLIGHT`
- Reply: `{ ok, status, counterpart }` where counterpart includes `responsive`, `role`, `provider`, `version`, and `composerAvailable`
- [ ] **Step 1: Write preflight behavior tests**

Cover responsive counterpart, missing counterpart, stale counterpart, send failure, and same-role/self-ping rejection. Prove the content reply only reads adapter state.

- [ ] **Step 2: Confirm RED**

Run: `node --test runtime/extension/tests/preflight.test.js runtime/extension/tests/session-status.test.js runtime/extension/tests/validation.test.js`
Expected: missing preflight module and missing message handlers.

- [ ] **Step 3: Implement the service-worker ping and content reply**

Authorize the requester through existing session ownership. Resolve the opposite role from the registry, send a bounded tab message, and return a normalized status. Do not mutate registry or queue state.

- [ ] **Step 4: Use active preflight for F11 and registration link state**

F11 calls `PMIA_RUN_PREFLIGHT`. Successful registration renders `LINK OK`, `WAITING ...`, or `FINAL QUEUED` from returned session status. Transient route messages still restore to the latest stable link label.

- [ ] **Step 5: Verify and commit**

Run focused tests and the full extension gate.
Commit message: `feat: add active PMIA counterpart preflight`

### Task 4: Release, live matrix, and cleanup

**Files:**
- Modify: `runtime/extension/manifest.json`
- Modify: `runtime/extension/README.md`
- Create: `docs/superpowers/plans/2026-07-29-runtime-0.5.1-verification.md`

- [ ] **Step 1: Version 0.5.1 and document operator behavior**

Document the fatal banner, automatic link state, active F11 preflight, and exact recovery action.

- [ ] **Step 2: Run the exact-tree automated gate**

Run `npm test`, `npm run validate`, `runtime\Validate_Extension_Runtime.ps1`, `git diff --check`, BOM scan, secret scan, and legacy-file guards.

- [ ] **Step 3: Reload Edge and clear stale extension errors**

Use Edge’s Extensions UI. Do not edit Edge preferences directly.
- [ ] **Step 4: Run the four-combination text matrix**

Launch temporary managed pairs for ChatGPT→Claude, Claude→ChatGPT, ChatGPT→ChatGPT, and Claude→Claude. Submit one unique marker in each sender, verify one exact receiver submission, and run active preflight from both roles.

- [ ] **Step 5: Voice smoke and soak checks**

Confirm ChatGPT and Claude sender controls initialize, no interim or boundary-only event is submitted, and a short repeated-turn soak shows no duplicate routing or growing preview state. Do not retain audio.

- [ ] **Step 6: Commit, merge, publish, and verify remote refs**

Commit release metadata, fast-forward canonical `main` only after green verification, push `main` and the feature branch, and create tag `pmia-runtime-v0.5.1`.

- [ ] **Step 7: Clean agent-created artifacts**

Close only temporary PMIA and Extensions windows. Remove temporary scripts, marker files, and the linked implementation worktree after merge. Preserve the compatibility junction while Edge still registers that path, and preserve every legacy runtime/evidence file requested by the user.

## Final response

Report:

- root cause and evidence
- features implemented
- exact commit, branch, and tag
- automated and live verification results
- files deliberately preserved
- cleanup completed
- any genuinely unverified risk, without claiming success beyond evidence